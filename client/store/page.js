const state = () => {
  // breadcrumbs: [],
}

const mutations = {
  // setBreadcrumbs(state, breadcrumbs) {
  //   state.breadcrumbs = breadcrumbs
  // }
}

const getters = {
  // getBreadcrumbs: (state) => state.breadcrumbs,
}

export default {
  state,
  mutations,
  getters
}